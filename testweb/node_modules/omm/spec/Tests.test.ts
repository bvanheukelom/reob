///<reference path="../typings/jasmine/jasmine.d.ts"/>

import * as omm from "../src/omm"
import * as Tests from "./classes/Tests"
import * as Mocks from "./Mocks"

// this class is used by the node tests that test omm

import TestSpec from "./Tests.spec"
import ServerSpec from "./ServerTests.spec"
import ClientSpec from "./ClientTests.spec"

describe("Node test:", function(){
    beforeAll(function () {
        Mocks.init();
        Tests.TestTreeCollection.init();
        Tests.TestPersonCollection.init();
        Mocks.mockMeteor.runAllStartup();
        omm.MeteorPersistence.init();
    });

    ServerSpec();
    TestSpec();
    ClientSpec();

});
